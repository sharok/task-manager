"use strict"

var React = require('react');

module.exports = React.createClass({
    render: function () {
        return (<div className="upcoming-task">
            нет ближайших задач
        </div>);
    }
});